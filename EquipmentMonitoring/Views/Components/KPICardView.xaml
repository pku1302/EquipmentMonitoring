<UserControl x:Class="EquipmentMonitoring.Views.Components.KPICardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border
        Padding="16,12"
        Background="White"
        CornerRadius="10"
        BorderBrush="#E5E7EB"
        BorderThickness="1">

        <Border.Effect>
            <DropShadowEffect
                BlurRadius="8"
                ShadowDepth="1"
                Opacity="0.05"/>
        </Border.Effect>

        <StackPanel>

            <TextBlock
                Text="{Binding Title}"
                FontSize="13"
                Foreground="#6B7280"/>

            <StackPanel
                Orientation="Horizontal"
                Margin="0,6,0,0">

                <TextBlock
                    Text="{Binding Value, StringFormat={}{0:N0}}"
                    FontSize="25"
                    FontWeight="SemiBold"
                    Foreground="#111827"/>

                <TextBlock
                    Text="{Binding Unit}"
                    FontSize="13"
                    Foreground="#6B7280"
                    VerticalAlignment="Bottom"
                    Margin="5,0,0,4"/>

            </StackPanel>

        </StackPanel>

    </Border>
</UserControl>
