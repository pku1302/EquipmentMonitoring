<UserControl x:Class="EquipmentMonitoring.Views.Chart.SensorChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EquipmentMonitoring.Views.Chart"
             mc:Ignorable="d" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
>

    <Border
        Margin="8"
        Padding="20"
        Background="White"
        CornerRadius="12"
        BorderBrush="#E5E7EB"
        BorderThickness="1">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel>

                <TextBlock
                    Text="{Binding Title}"
                    FontSize="20"
                    FontWeight="SemiBold"
                    Foreground="#111827"/>

                <TextBlock
                    Text="{Binding Description}"
                    FontSize="12"
                    Foreground="#6B7280"
                    Margin="0,4,0,0"/>

            </StackPanel>

            <StackPanel
                Grid.Row="1"
                Orientation="Horizontal"
                Margin="0,15,0,10">

                <TextBlock
                    Text="{Binding CurrentValue,
                                   StringFormat={}{0:F1}}"
                    FontSize="28"
                    FontWeight="Bold"/>

                <TextBlock
                    Text="{Binding Unit}"
                    FontSize="16"
                    Foreground="#6B7280"
                    VerticalAlignment="Bottom"
                    Margin="4,0,0,4"/>

            </StackPanel>

            <lvc:CartesianChart
                Grid.Row="2"
                Series="{Binding Series}"
                XAxes="{Binding XAxes}"
                YAxes="{Binding YAxes}"
                ZoomMode="X"
                Height="280"/>

        </Grid>

    </Border>

</UserControl>
