<UserControl x:Class="EquipmentMonitoring.Views.AlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EquipmentMonitoring.Views"
             mc:Ignorable="d" >
    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Alarm History"
            FontSize="28"
            FontWeight="Bold"/>

        <Button
            Grid.Row="1"
            Content="Refresh"
            Command="{Binding RefreshCommand}"
            Width="100"
            Height="35"
            HorizontalAlignment="Left"
            Margin="0,20"/>

        <DataGrid
            Grid.Row="2"
            ItemsSource="{Binding Alarms}"
            AutoGenerateColumns="False"
            IsReadOnly="True">
            
            <DataGrid.Columns>

                <DataGridTextColumn 
                    Header="Time"
                    Binding="{Binding OccurredAt,
                              StringFormat=yyyy-MM-dd HH:mm:ss}"
                    Width="180"/>

                <DataGridTextColumn 
                    Header="Equipment"
                    Binding="{Binding EquipmentId}"
                    Width="120"/>

                <DataGridTextColumn 
                    Header="Code"
                    Binding="{Binding AlarmCode}"
                    Width="130"/>

                <DataGridTextColumn 
                    Header="Message"
                    Binding="{Binding AlarmMessage}"
                    Width="*"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
