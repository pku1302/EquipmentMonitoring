<UserControl x:Class="EquipmentMonitoring.Views.Pages.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EquipmentMonitoring.Views.Pages"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <ItemsControl ItemsSource="{Binding Equipments}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                    Width="220"
                    Margin="10"
                    Padding="16"
                    CornerRadius="8"
                    BorderThickness="1">

                        <StackPanel>

                            <TextBlock
                            Text="{Binding EquipmentId}"
                            FontSize="20"
                            FontWeight="Bold" />

                            <TextBlock
                            Margin="0,8,0,0"
                            Text="{Binding Status}" />

                            <TextBlock
                            Margin="0,12,0,0">
                            <Run Text="Temperature: " />
                            <Run Text="{Binding Temperature}" />
                            <Run Text=" ℃" />
                            </TextBlock>

                            <TextBlock>
                            <Run Text="Pressure: " />
                            <Run Text="{Binding Pressure}" />
                            </TextBlock>

                            <TextBlock>
                            <Run Text="RPM: " />
                            <Run Text="{Binding MotorRpm}" />
                            </TextBlock>

                            <TextBlock>
                            <Run Text="Production: " />
                            <Run Text="{Binding ProductionCount}" />
                            </TextBlock>

                            <TextBlock
                            Margin="0,8,0,0"
                            FontSize="11"
                            Text="{Binding Timestamp}" />

                        </StackPanel>
                    </Border>
                </DataTemplate>    
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
    
</UserControl>
